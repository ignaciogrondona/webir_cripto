<%= form_with(model: criptocurrency_history, local: true) do |form| %>
  <% if criptocurrency_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(criptocurrency_history.errors.count, "error") %> prohibited this criptocurrency_history from being saved:</h2>

      <ul>
      <% criptocurrency_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
